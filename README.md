# spec-lock-canary

A throwaway public repository for testing [spec-lock](https://github.com/EvanAgee/spec-lock) on GitHub. Every file here is made up.

`main` requires the `spec-lock` check. A commit reaches `main` by being pushed to a `land/*` branch first, where the check runs, and then to `main`.
